<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_crm_opportunity_rule_generate_suggestions" model="ir.cron">
        <field name="name">CRM: generar sugerencias de oportunidad</field>
        <field name="model_id" ref="model_crm_opportunity_rule"/>
        <field name="state">code</field>
        <field name="code">model._cron_generate_suggestions()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
    </record>

</odoo>
